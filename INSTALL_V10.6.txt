GARANG V10.6 — AUTH-FIRST STABILITY OVERWRITE

1) 압축을 푼다.
2) 이 폴더 안의 파일/폴더 전체를 기존 GitHub 저장소 ROOT에 덮어쓴다.
   - GARANG_V10.6_STABILITY_OVERWRITE 폴더 자체를 한 단계 더 넣지 않는다.
   - index.html이 저장소 ROOT에 바로 있어야 한다.
3) GitHub Pages가 배포되면 Safari에서 강력 새로고침/사이트 데이터 삭제 후 다시 연다.

이번 버전의 핵심 변경:
- V10.4/V10.5의 중첩 openPage/auth wrapper 제거
- AUTH가 최상위 라우팅 권한을 갖도록 단일 V10.6 router 적용
- 로그인 전 모든 앱 페이지/하단 네비 차단
- stale hash로 로그인 화면 우회하는 문제 차단
- 로그인/회원가입 submit을 명시적으로 연결
- Firebase Auth 실패 시 원인을 화면에 표시
- GARANG 로고를 외부 SVG 경로 의존 없이 inline SVG로 변경
- 화면 하단 버전을 V10.6으로 변경
- 기존 운동/식단/러닝/AI/학습/메모리/인증 기능 파일은 유지

Firebase Console 확인:
- Authentication > Sign-in method > Email/Password 활성화
- Authorized domains에 jang-sang.github.io의 실제 Pages 도메인 등록
- Firestore Database 활성화
