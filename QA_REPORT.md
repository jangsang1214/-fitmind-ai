# GARANG V10.9.1 1차 대규모 업데이트 최종 QA

검사일: 2026-09-01  
대상: `GARANG_V10.9.1_MAJOR_UPDATE_COMPLETE`

## 판정

배포를 막던 로컬 자산 경로 오류와 데이터 회귀를 수정했다. 자동 단위·회귀 검사, 정적 무결성 검사, 로컬 HTTP 실행 검사, 320px/390px 모바일 브라우저 검사를 모두 통과했으므로 현재 소스는 GitHub Pages에 커밋 가능한 상태다.

## 수정한 실제 원인

1. 분류 폴더로 이동한 파일을 루트 `index.html`이 계속 이전 경로로 요청해 CSS/JS/이미지 일부가 로드되지 않았다. 배포용 루트 파일을 복구하고 분류 소스와 동기화 검사를 추가했다.
2. 로그인 시 게스트/데모 상태를 계정 저장소로 안전하게 넘기는 절차가 없었다. 첫 로그인에만 복사하고 게스트 백업은 유지하며 기존 계정과 일반 신규 계정은 분리했다.
3. 클라우드 복원이 로컬 전체 상태를 덮어써 빈 프로필이나 빈 운동 배열이 최신 로컬 기록을 지울 수 있었다. 컬렉션별 ID 병합과 항목 타임스탬프 충돌 해결로 변경했다.
4. JavaScript의 `Number(null) === 0` 동작 때문에 미입력 체중이 0으로 변환되어 단백질 목표가 0 g으로 표시됐다. `null`과 공백을 결측값으로 유지하도록 공통 숫자 변환을 수정했다.
5. 기록이 부족한 Performance Score를 AI 화면이 `null/100`으로 출력했다. 계산 불가 상태를 명시하는 한국어/영어 응답으로 교체했다.
6. 이전 산식 점수와 새 산식 점수를 한 그래프와 증감 비교에 섞었다. 점수에 산식 버전을 저장하고 같은 버전끼리만 비교하도록 변경했다.

## 자동검사 결과

- 코어 단위검사: 15건 통과
- 어댑터 단위검사: 8건 통과
- 대규모 업데이트 회귀검사: 13건 통과
- 합계: 36건 통과, 실패 0건
- JavaScript 구문 검사: 85개 파일 통과
- JSON 검사: 26개 파일 통과
- JSONL 검사: 11개 파일 통과
- 활성 자산 경로 검사: 27개 참조 통과

회귀검사는 인바디 3개 입력 자동 계산, 키 미입력 시 BMI 보류, 결측값의 0 변환 방지, 기존 기록과 자동 추정 기록의 라벨 분리, 데모 계정 이전, 계정 격리, 로컬·클라우드 고유 기록 보존, 동일 ID 최신 항목 선택, 점수 산식 버전, AI 결측 응답, 앱의 안전 함수 사용, 루트·분류 소스 동기화를 직접 확인한다.

## 실제 브라우저 검사

- 로컬 HTTP 서버에서 CSS 5개, JavaScript, 데이터 파일, 로고, 아이콘, manifest 및 service worker 요청이 정상 응답했다.
- 390×844 화면에서 데모 진입, AI 점수 결측 응답, 단백질 목표 107 g 계산, InBody 70 kg 저장, 플래너 저장, 장기 기억 저장 및 새로고침 후 보존을 확인했다.
- 320×844 화면에서 홈, 운동, 식단, 러닝, 플래너, AI, FREE/PRO, 프로필, InBody, Performance Score, 장기 기억, 업적, 설정의 13개 화면을 실제 메뉴로 열었다.
- 13개 화면 모두 중복 ID 0건, 가로 넘침 0건, 콘솔 오류·경고 0건이었다.
- 깨끗한 브라우저 저장소와 320×844 화면에서 체중 80 kg, 체지방률 25%, 골격근량 32 kg만 입력했다. 체지방량 20 kg, BMI 24.7, 기초대사량 추정 1666 kcal가 자동 산출되고 저장 이력에 동일하게 표시되는 것을 확인했다.

## 외부 환경에서 별도 확인할 항목

Firebase 실제 계정 동기화, 실제 OCR/LLM API, 결제 제공자, 실기기 GPS 및 iOS PWA 설치는 외부 인증 정보와 기기가 필요한 통합 항목이다. 이번 판정은 해당 연동을 모의 어댑터 경계와 실패 처리까지 검사한 결과이며, 운영 자격 증명을 사용한 결제·AI·GPS 성공까지 보증한다는 뜻은 아니다.
