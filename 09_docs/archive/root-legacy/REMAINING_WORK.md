# 운영 환경 인수 체크리스트

현재 정적 앱 코어와 1차 대규모 업데이트는 자동검사와 모바일 브라우저 검사를 통과했다. 아래 항목은 소스 오류 때문에 남은 작업이 아니라 외부 계정, 운영 자격 증명 또는 실제 기기가 있어야 확인할 수 있는 배포 후 인수 항목이다.

1. **Firebase:** 승인된 테스트 계정으로 로그인, 로그아웃, 신규 계정 데모 이전, 기존 계정 격리, 두 기기 간 동일 ID 충돌을 확인한다. 운영 도메인을 Firebase Authorized domains에 등록하고 Firestore rules를 검토한다.
2. **LLM 백엔드:** 같은 출처의 인증된 서버 엔드포인트를 연결하고 정상 응답, 지연, 4xx/5xx, 잘못된 JSON, 개인정보 보존 정책을 확인한다. 로컬 규칙 코치는 LLM 성공으로 표시하지 않는다.
3. **OCR 백엔드:** 실제 InBody 이미지 표본으로 추출 정확도와 공급자 응답 매핑을 확인한다. OCR 결과는 사용자가 검토한 뒤에만 저장한다.
4. **결제:** 서버 검증 webhook과 서버 소유 entitlement를 연결하고 성공, 취소, 재시도, 환불을 테스트한다. 브라우저의 로컬 `plan` 값은 접근 권한의 근거로 사용하지 않는다.
5. **실기기:** iPhone Safari와 Android Chrome에서 카메라, 소프트 키보드 safe area, GPS 권한, 백그라운드 동작, 알림, PWA 설치·업데이트·오프라인 재실행을 확인한다.

이번 파일은 위 공급자별 운영 설정을 포함하지 않는다. 연결 전에도 앱은 데모 및 로컬 저장 모드에서 실행되며, 외부 서비스 실패를 성공으로 표시하지 않는다.
