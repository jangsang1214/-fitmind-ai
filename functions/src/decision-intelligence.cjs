'use strict';

const ENGINE_VERSION='decision-intelligence-v1';
const MODES=Object.freeze(['collect_data','caution','recover','reduce','maintain','progress','goal_focus']);
const object=v=>!!v&&typeof v==='object'&&!Array.isArray(v);
const rows=v=>Array.isArray(v)?v.filter(object):[];
const clamp=(v,min,max)=>Math.max(min,Math.min(max,Number(v)||0));
const round=(v,d=2)=>{const p=10**d;return Math.round((Number(v)+Number.EPSILON)*p)/p;};
const has=(list,value)=>Array.isArray(list)&&list.includes(value);
const patternIds=s=>new Set(rows(s?.patterns).map(p=>String(p.id||'')));
const MODE_POLICY=Object.freeze({collect_data:{priority:55,intensityScale:null,volumeScale:null,duration:null,type:null},caution:{priority:100,intensityScale:0,volumeScale:0,duration:null,type:null},recover:{priority:90,intensityScale:.5,volumeScale:.55,duration:30,type:'recovery'},reduce:{priority:75,intensityScale:.7,volumeScale:.7,duration:40,type:'workout'},maintain:{priority:30,intensityScale:1,volumeScale:1,duration:50,type:'workout'},progress:{priority:45,intensityScale:1.05,volumeScale:1.05,duration:55,type:'workout'},goal_focus:{priority:60,intensityScale:.9,volumeScale:.9,duration:50,type:'workout'}});
const TITLES=Object.freeze({recover:{ko:'회복 중심 세션',en:'Recovery-focused session'},reduce:{ko:'강도 조정 세션',en:'Reduced-load session'},maintain:{ko:'계획된 훈련 세션',en:'Planned training session'},progress:{ko:'점진적 향상 세션',en:'Progression session'},goal_focus:{ko:'목표 집중 세션',en:'Goal-focused session'}});
const COPY=Object.freeze({collect_data:{ko:'판단할 데이터가 아직 부족합니다. 최근 체크인과 기록을 더 쌓아 주세요.',en:'There is not enough recent data for a confident training decision yet.'},caution:{ko:'통증·주의 신호가 있어 훈련 강도를 자동 제안하지 않습니다.',en:'A pain/caution signal is present, so GARANG will not suggest training intensity automatically.'},recover:{ko:'최근 부하와 회복 신호를 함께 보면 회복 중심 선택이 우선입니다.',en:'Recent load and recovery signals favor a recovery-focused choice.'},reduce:{ko:'오늘은 평소보다 강도나 볼륨을 낮추는 편이 적절합니다.',en:'Reducing intensity or volume is the safer fit for today.'},maintain:{ko:'현재 신호에서는 계획한 훈련을 유지할 수 있는 범위입니다.',en:'Current signals support keeping the planned training level.'},progress:{ko:'회복과 부하가 안정적이라 작은 점진적 향상을 시도할 수 있습니다.',en:'Recovery and load are stable enough for a small, controlled progression.'},goal_focus:{ko:'현재 상태는 훈련 가능하지만 목표와 최근 행동의 정렬을 더 높일 필요가 있습니다.',en:'You can train, but recent behavior could align more closely with your goal.'}});
function memorySummary(memoryContext){const entries=rows(memoryContext?.entries),active=entries.filter(x=>x.status!=='superseded'&&x.status!=='expired'&&x.userConfirmed!==false);return {evidenceCount:active.length,goalMemory:active.some(x=>String(x.type||x.category)==='goal'),preferenceCount:active.filter(x=>String(x.memoryClass||x.type)==='preference'||String(x.type)==='preference').length};}
function chooseMode(s){const confidence=clamp(s?.confidence,0,1),readiness=s?.readiness||{},fatigue=s?.fatigue||{},load=s?.load||{},goal=s?.goalAlignment||{},patterns=patternIds(s),pain=has(readiness.reasons,'PAIN_CAUTION')||has(fatigue.reasons,'PAIN_CAUTION');if(pain)return 'caution';if((readiness.value==null&&confidence<.35)||confidence<.18)return 'collect_data';if(fatigue.band==='very_high'||(load.band==='spike'&&['low','guarded'].includes(readiness.band))||patterns.has('high_fatigue_proxy'))return 'recover';if(fatigue.band==='high'||readiness.band==='low'||load.band==='spike'||patterns.has('sleep_debt')||patterns.has('fatigue_cluster'))return 'reduce';if(goal.score!=null&&['low','mixed'].includes(goal.band)&&Number(goal.confidence)>=.4)return 'goal_focus';if(readiness.band==='high'&&fatigue.band==='low'&&load.band==='stable'&&confidence>=.55)return 'progress';return 'maintain';}
function reasonsFor(s,mode){const out=[`MODE_${mode.toUpperCase()}`],readiness=s?.readiness||{},fatigue=s?.fatigue||{},load=s?.load||{},goal=s?.goalAlignment||{},patterns=patternIds(s);for(const code of readiness.reasons||[])if(['PAIN_CAUTION','LOW_ENERGY','SHORT_SLEEP','HIGH_SORENESS','HIGH_STRESS','CHECKIN_STABLE'].includes(code))out.push(code);if(load.band==='spike')out.push('LOAD_SPIKE');if(load.band==='drop')out.push('LOAD_DROP');if(['high','very_high'].includes(fatigue.band))out.push('FATIGUE_ELEVATED');for(const id of ['sleep_debt','fatigue_cluster','training_load_spike','training_load_drop','consistency_drop','low_readiness','high_fatigue_proxy'])if(patterns.has(id))out.push(`PATTERN_${id.toUpperCase()}`);if(goal.score!=null)out.push(`GOAL_${String(goal.band||'unknown').toUpperCase()}`);return [...new Set(out)].slice(0,10);}
function actionProposal(mode,reasons,confidence){if(!TITLES[mode]||confidence<.25)return null;const p=MODE_POLICY[mode];return {tool:'createPlan',requiresConfirmation:true,title:TITLES[mode],args:{type:p.type,duration:p.duration,intensityScale:p.intensityScale,volumeScale:p.volumeScale,decisionEngineVersion:ENGINE_VERSION,decisionMode:mode,reasonCodes:reasons.slice(0,8)}};}
function decide(userStateInput,{memoryContext=null}={}){const s=object(userStateInput)?userStateInput:{},mode=chooseMode(s),policy=MODE_POLICY[mode],confidence=round(clamp(s.confidence,0,1),2),reasons=reasonsFor(s,mode),memory=memorySummary(memoryContext),highPattern=rows(s.patterns).some(p=>p.severity==='high'&&Number(p.confidence)>=.5),surface=mode!=='maintain'||highPattern;return {engineVersion:ENGINE_VERSION,decisionId:`${String(s.asOf||'unknown')}:${mode}:${reasons.slice(0,3).join('|')}`,asOf:String(s.asOf||''),mode,priority:policy.priority,confidence,surface,reasonCodes:reasons,summary:COPY[mode],signals:{readinessBand:String(s?.readiness?.band||'unknown'),readinessValue:s?.readiness?.value??null,fatigueBand:String(s?.fatigue?.band||'unknown'),fatigueScore:s?.fatigue?.score??null,loadBand:String(s?.load?.band||'unknown'),loadRatio:s?.load?.ratio??null,goalBand:String(s?.goalAlignment?.band||'unknown'),goalScore:s?.goalAlignment?.score??null},recommendation:{intensityScale:policy.intensityScale,volumeScale:policy.volumeScale,duration:policy.duration,type:policy.type},actionProposal:actionProposal(mode,reasons,confidence),memoryEvidence:memory,guardrails:{requiresConfirmation:true,noSilentMutation:true,painCautionBlocksIntensity:true,medicalDiagnosis:false}};}
function compactForContext(d){if(!object(d))return null;return {engineVersion:d.engineVersion,decisionId:d.decisionId,asOf:d.asOf,mode:d.mode,priority:d.priority,confidence:d.confidence,surface:d.surface,reasonCodes:d.reasonCodes,summary:d.summary,signals:d.signals,recommendation:d.recommendation,actionProposal:d.actionProposal,memoryEvidence:d.memoryEvidence,guardrails:d.guardrails};}
function diagnostics(userState,options={}){const d=decide(userState,options);return {engineVersion:d.engineVersion,asOf:d.asOf,mode:d.mode,priority:d.priority,confidence:d.confidence,surface:d.surface,reasonCount:d.reasonCodes.length,hasActionProposal:!!d.actionProposal,guardrails:d.guardrails};}
module.exports=Object.freeze({ENGINE_VERSION,MODES,MODE_POLICY,COPY,decide,compactForContext,diagnostics});
