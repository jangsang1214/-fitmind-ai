GARANG V10.1 — GitHub Pages drop-in deployment

IMPORTANT:
- Upload/extract the FILES, not the ZIP itself.
- index.html must be in the repository root.
- Keep .nojekyll in the repository root.
- GitHub Pages: Settings > Pages > Deploy from branch > main / root.
- Do not upload this archive as index.html.

This build also fixes:
- inline saveCert() not being globally callable
- workout verification now keeps its workoutId
- safer localStorage access
- runtime error logging instead of silent failures

This is still a static-browser build. Real Google/Apple/Firebase auth and server-side data sync are not included because no backend credentials/config were supplied.
