# GARANG V9.5.2 Global AI Patch
- Signup now captures country/region, language, and units and persists them to profile/preferences.
- Auth touch safety added for mobile Safari/Chrome; known stale modal layers are closed while auth is active.
- Server LLM settings remain hidden in V9.5; V10 server LLM is not exposed.
- AI chat uses a unified local-first orchestration layer: personal state + Today Coach context + learned Knowledge, with automatic external search when the existing Knowledge layer determines it is needed.
- Search results are retained in Knowledge for retrieval reuse.
- Removed empty index.html.tmp artifact.
