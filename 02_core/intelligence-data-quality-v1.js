(function(root,factory){const api=factory();if(typeof module==='object'&&module.exports)module.exports=api;if(root)root.GarangIntelligenceDataQualityV1=api;})(typeof globalThis!=='undefined'?globalThis:this,function(){
'use strict';
const VERSION='intelligence-data-quality-v1.0.0';
const object=v=>!!v&&typeof v==='object'&&!Array.isArray(v),list=v=>Array.isArray(v)?v.filter(object):[],finite=v=>v!==null&&v!==undefined&&v!==''&&Number.isFinite(Number(v))?Number(v):null,round=(v,d=3)=>{const p=10**d;return Math.round((Number(v)+Number.EPSILON)*p)/p;},clamp=(v,min,max)=>Math.max(min,Math.min(max,Number(v)||0));
function build(episodesInput={},options={}){
 const episodes=list(episodesInput?.episodes||episodesInput),asOf=String(options.asOf||episodesInput?.asOf||new Date().toISOString().slice(0,10)),ids=new Set(),duplicates=[],future=[],invalid=[],chainBreaks=[];
 let attributed=0,resolved=0,executed=0,outcomes=0;
 for(const ep of episodes){const id=String(ep?.episodeId||'');if(!id)invalid.push('MISSING_EPISODE_ID');else if(ids.has(id))duplicates.push(id);else ids.add(id);if(String(ep?.date||'')>asOf)future.push(id||String(ep?.date));const ac=finite(ep?.attribution?.confidence);if(ac!==null&&ac>=.5)attributed++;if(ep?.userResponse?.status&&!['unresolved',''].includes(String(ep.userResponse.status)))resolved++;if(ep?.execution?.executionId||ep?.execution?.status==='observed'||(finite(ep?.execution?.completionRatio)??0)>0)executed++;if(ep?.outcome?.outcomeId||finite(ep?.outcome?.score)!==null||ep?.outcome?.classification)outcomes++;if(ep?.outcome?.outcomeId&&!ep?.execution?.executionId)chainBreaks.push({episodeId:id,code:'OUTCOME_WITHOUT_EXECUTION'});if(ep?.execution?.executionId&&!ep?.recommendation?.recommendationId)chainBreaks.push({episodeId:id,code:'EXECUTION_WITHOUT_RECOMMENDATION'});}
 const n=episodes.length,coverage={attribution:n?round(attributed/n):0,resolution:n?round(resolved/n):0,execution:n?round(executed/n):0,outcome:n?round(outcomes/n):0},integrityPenalty=duplicates.length+future.length+invalid.length+chainBreaks.length,coverageScore=round((coverage.attribution*.3+coverage.resolution*.2+coverage.execution*.25+coverage.outcome*.25)),score=round(clamp(coverageScore-integrityPenalty*.12,0,1)),status=integrityPenalty?'invalid':n<3?'insufficient':score>=.7?'strong':score>=.45?'usable':'sparse';
 return Object.freeze({version:VERSION,asOf,inputEpisodes:n,status,score,coverage:Object.freeze(coverage),issues:Object.freeze({duplicateEpisodeIds:Object.freeze(duplicates),futureEpisodes:Object.freeze(future),invalid:Object.freeze(invalid),chainBreaks:Object.freeze(chainBreaks)}),guardrails:Object.freeze({futureEvidenceRejected:true,duplicateAware:true,chainIntegrityChecked:true,noDataFabrication:true})});
}
function compactForContext(v={}){return {version:String(v.version||VERSION),asOf:String(v.asOf||''),inputEpisodes:Number(v.inputEpisodes)||0,status:String(v.status||'insufficient'),score:finite(v.score)??0,coverage:object(v.coverage)?v.coverage:{},issues:object(v.issues)?{duplicateEpisodeIds:list(v.issues.duplicateEpisodeIds).slice(0,4),futureEpisodes:list(v.issues.futureEpisodes).slice(0,4),invalid:list(v.issues.invalid).slice(0,4),chainBreaks:list(v.issues.chainBreaks).slice(0,4)}:{},guardrails:{futureEvidenceRejected:true,duplicateAware:true,chainIntegrityChecked:true,noDataFabrication:true}};}
return Object.freeze({VERSION,build,compactForContext});
});
