/* Same-origin backend endpoints only. Never put provider secrets in browser code. */
window.GARANG_SERVICES_CONFIG={llmEndpoint:null,ocrEndpoint:null,paymentEndpoint:null,apiBase:null,environment:'development',analyticsConsent:false,timeoutMs:30000};
